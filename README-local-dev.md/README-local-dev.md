# Local dev with Docker (FastAPI + Postgres + React PWA)

Prerequisites:
- Docker & Docker Compose installed
- VS Code (optional)

1) Prepare env file
- Copy backend/.env.example -> backend/.env
- Optionally change SECRET_KEY.

2) Build and start services
From the project root run: